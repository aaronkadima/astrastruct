function patch(){const menus=[...document.querySelectorAll<HTMLElement>('.modern-menu')],menu=menus.find(m=>m.querySelector<HTMLElement>('.modern-menu-button')?.textContent?.trim()==='Resultados')||menus.find(m=>m.querySelector<HTMLElement>('.modern-menu-button')?.textContent?.trim()==='Modelo');if(!menu)return;const popup=menu.querySelector<HTMLElement>('.modern-menu-popup');if(!popup||popup.querySelector('[data-combination-envelope-open]'))return;const button=document.createElement('button');button.type='button';button.dataset.combinationEnvelopeOpen='true';button.setAttribute('data-testid','combination-envelope-open');button.innerHTML='<span>Combinações & envoltórias…</span><kbd>v0.53.8</kbd>';button.addEventListener('click',()=>{menu.classList.remove('open');window.dispatchEvent(new CustomEvent('astrastruct:combination-envelope-open'))});const elu=popup.querySelector('[data-elu-els-dashboard-open]');if(elu?.nextSibling)popup.insertBefore(button,elu.nextSibling);else popup.append(button)}
const observer=new MutationObserver(patch);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',patch,{once:true});else patch();
export{};