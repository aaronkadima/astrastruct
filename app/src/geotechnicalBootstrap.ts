function patch(){document.querySelectorAll<HTMLElement>('.modern-menu').forEach(menu=>{const trigger=menu.querySelector<HTMLElement>('.modern-menu-button');if(trigger?.textContent?.trim()!=='Modelo')return;const popup=menu.querySelector<HTMLElement>('.modern-menu-popup');if(!popup||popup.querySelector('[data-geotechnical-open]'))return;const button=document.createElement('button');button.type='button';button.dataset.geotechnicalOpen='true';button.setAttribute('data-testid','geotechnical-open');button.innerHTML='<span>Geotecnia · perfis de solo…</span><kbd>v0.53.5</kbd>';button.addEventListener('click',()=>{menu.classList.remove('open');window.dispatchEvent(new CustomEvent('astrastruct:geotechnical-open'))});const ssi=popup.querySelector('[data-ssi-open]');if(ssi?.nextSibling)popup.insertBefore(button,ssi.nextSibling);else popup.prepend(button)})}
const observer=new MutationObserver(patch);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',patch,{once:true});else patch();
export{};