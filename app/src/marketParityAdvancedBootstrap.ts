function patch(){document.querySelectorAll<HTMLElement>('.modern-menu').forEach(menu=>{const trigger=menu.querySelector<HTMLElement>('.modern-menu-button');if(trigger?.textContent?.trim()!=='Modelo')return;const popup=menu.querySelector<HTMLElement>('.modern-menu-popup');if(!popup||popup.querySelector('[data-market-parity-advanced-open]'))return;const button=document.createElement('button');button.type='button';button.dataset.marketParityAdvancedOpen='true';button.setAttribute('data-testid','market-parity-advanced-open');button.innerHTML='<span>Explorador do edifício + fundações 3D…</span><kbd>v0.53.1</kbd>';button.addEventListener('click',()=>{menu.classList.remove('open');window.dispatchEvent(new CustomEvent('astrastruct:market-parity-advanced-open'))});const base=popup.querySelector('[data-market-parity-open]');if(base?.nextSibling)popup.insertBefore(button,base.nextSibling);else popup.prepend(button)})}
const observer=new MutationObserver(patch);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',patch,{once:true});else patch();
export{};
