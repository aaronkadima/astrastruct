function patch(){document.querySelectorAll<HTMLElement>('.modern-menu').forEach(menu=>{const trigger=menu.querySelector<HTMLElement>('.modern-menu-button');if(trigger?.textContent?.trim()!=='Modelo')return;const popup=menu.querySelector<HTMLElement>('.modern-menu-popup');if(!popup||popup.querySelector('[data-wind-durability-open]'))return;const button=document.createElement('button');button.type='button';button.dataset.windDurabilityOpen='true';button.setAttribute('data-testid','wind-durability-open');button.innerHTML='<span>Durabilidade + vento auditável…</span><kbd>v0.53.2</kbd>';button.addEventListener('click',()=>{menu.classList.remove('open');window.dispatchEvent(new CustomEvent('astrastruct:wind-durability-open'))});const advanced=popup.querySelector('[data-market-parity-advanced-open]');if(advanced?.nextSibling)popup.insertBefore(button,advanced.nextSibling);else popup.prepend(button)})}
const observer=new MutationObserver(patch);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',patch,{once:true});else patch();
export{};
