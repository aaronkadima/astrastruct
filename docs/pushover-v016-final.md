# v0.16 — implementação consolidada

A implementação consolidada usa controle de deslocamento com sistema aumentado, mantém o controle de carga anterior, integra o Newton constitutivo das rótulas de fibras, registra o caminho `λ–u` e expõe a curva capacidade no pós-processamento. Arc-length permanece explicitamente fora do escopo desta versão.
