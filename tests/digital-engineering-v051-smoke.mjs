import assert from'node:assert/strict';
import{DIGITAL_ENGINEERING_RECORD_CONTRACT,DIGITAL_ENGINEERING_RECORD_VERSION,projectFingerprint,createDigitalEngineeringRecord,exportDigitalEngineeringRecord}from'../web/src/automation/index.js';
const project={id:'P1',name:'digital-record',schemaVersion:2,nodes:[{id:'N1'}],elements:[],loadCases:[{id:'LC1'}],loadCombinations:[],settings:{analysisType:'linear',analysisScenarioId:'LC1'},meta:{productVersion:'0.50.0',updatedAt:'A'},results:{large:'ignored'},automationStudy:{lastRun:{ignored:true}}};
const fp1=projectFingerprint(project),fp2=projectFingerprint({...project,meta:{...project.meta,updatedAt:'B'},results:{different:true},automationStudy:{different:true}});assert.equal(fp1,fp2);assert.match(fp1,/^fnv1a32:[0-9a-f]{8}$/);
const study={contract:'project-automation-study/v1',version:'0.51.0-exp',name:'Audit',preset:'digital-audit',scenarioId:null,stopOnError:true};const steps=[{id:'advisor',type:'advisor',status:'success',startedAt:'t0',finishedAt:'t1',summary:{ok:true},error:null}];const advisor={provider:{id:'astra-rules-v1'},summary:{blockers:0,warnings:0,info:0,ok:1}};
const record=createDigitalEngineeringRecord(project,{executedAt:'2026-09-14T10:00:00.000Z',study,steps,advisor,status:'completed'});assert.equal(record.contract,DIGITAL_ENGINEERING_RECORD_CONTRACT);assert.equal(record.version,DIGITAL_ENGINEERING_RECORD_VERSION);assert.equal(record.project.fingerprint,fp1);assert.match(record.recordId,/^DER-[0-9a-f]{8}$/);const parsed=JSON.parse(exportDigitalEngineeringRecord(record));assert.equal(parsed.recordId,record.recordId);assert.equal(parsed.execution.steps.length,1);
console.log('AstraStruct v0.51 digital engineering smoke: stable project fingerprint and auditable execution record coherent.');
