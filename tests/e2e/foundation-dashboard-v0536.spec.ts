import{test,expect}from'@playwright/test';
test('foundation decision dashboard keeps normative, SSI and geotechnical evidence separate after v0.53.13 integration',async({page})=>{await page.goto('');await page.waitForFunction(()=>document.documentElement.dataset.astraReady==='true');await page.evaluate(()=>{localStorage.setItem('astrastruct.project',JSON.stringify({nodes:[{id:'N1',x:0,y:0,z:0}],settings:{analysisType:'linear'},foundationReview:{profile:{},items:[{id:'F1',label:'Sapata F1',nodeId:'N1',type:'footing',geometry:{B:2,L:2},demand:{N:200,Mx:0,My:0,Hx:0,Hy:0},soil:{qDesign:100},enabledChecks:{bearing:true,sliding:false,overturning:false,punching:false,oneWayShear:false,flexure:false}}]},foundationGeotechnical:{profiles:[{id:'SP1',label:'SP-01',x:0,y:0,groundZ:0,source:'Boletim SPT',method:'SPT',layers:[{id:'L1',label:'Areia',topDepthM:0,bottomDepthM:8,soilClass:'areia',parameters:{nSpt:12}}]}],links:[{foundationId:'F1',profileId:'SP1'}]}}));window.dispatchEvent(new CustomEvent('astrastruct:foundation-dashboard-open'))});const shell=page.getByTestId('foundation-decision-dashboard');await expect(shell).toBeVisible();await expect(shell.getByText('Fundação · SSI · Geotecnia · v0.53.13')).toBeVisible();const row=page.getByTestId('foundation-decision-F1');await expect(row).toBeVisible();await expect(row.getByText('READY FOR REVIEW')).toBeVisible();await expect(row.getByText('PASS',{exact:true})).toBeVisible();await expect(shell.getByText(/READY FOR REVIEW ≠ aprovação/)).toBeVisible();await expect(shell.getByTestId('foundation-reaction-context')).toHaveCount(0);await row.getByText('Diagnóstico e recomendações').click();await expect(row.getByText(/SSI: 0\/6 rigidezes/)).toBeVisible();});
