import{test,expect}from'@playwright/test';

test('SSI espacial abre em desktop e responsivo com governança explícita',async({page})=>{await page.goto('');await page.waitForFunction(()=>document.documentElement.dataset.astraReady==='true');await page.evaluate(()=>window.dispatchEvent(new CustomEvent('astrastruct:ssi-open')));const shell=page.getByTestId('foundation-ssi-workbench');await expect(shell).toBeVisible();await expect(shell.getByText('SSI espacial auditável · v0.53.4')).toBeVisible();await expect(shell.getByTestId('ssi-enabled')).toBeVisible();await expect(shell.getByTestId('ssi-save')).toBeVisible();await shell.getByRole('button',{name:'Auditoria',exact:true}).click();await expect(shell.getByText('Trilha de auditoria')).toBeVisible();await expect(shell.getByText(/R = −K·u/)).toBeVisible();await expect(shell.getByText(/linear, P‑Delta, modal e co‑rotacional 3D/)).toBeVisible();});

test('Canvas 3D principal mostra molas, recalque, reação e pressão SSI',async({page})=>{await page.goto('');const project={id:'e2e-ssi-overlay',name:'SSI overlay',version:13,schemaVersion:2,units:'kN-m-MPa',nodes:[{id:'N1',x:0,y:0,z:0},{id:'N2',x:0,y:0,z:3}],elements:[{id:'C1',type:'frame3d',n1:'N1',n2:'N2',materialId:'C',sectionId:'S',orientation:{up:[0,1,0]}}],materials:[{id:'C',type:'concrete',E:30000000,nu:.2,density:25}],sections:[{id:'S',family:'rect',A:.3,Iy:.006,Iz:.008,J:.003,I:.008}],supports:[{nodeId:'N1',ux:true,uy:true,uz:false,rx:true,ry:true,rz:true}],loads:[{id:'P',caseId:'LC1',nodeId:'N2',fx:0,fy:0,fz:-100,mx:0,my:0,mz:0}],elementLoads:[],settlements:[],nodeSprings:[{id:'SSI-F1',nodeId:'N1',kx:0,ky:0,kz:10000,krx:0,kry:0,krz:0,metadata:{generator:'astrastruct-foundation-ssi-v0533',contract:'foundation-ssi-model/v1',foundationId:'F1',source:'e2e'}}],nodalMasses:[],loadCases:[{id:'LC1',name:'Vertical',type:'user'}],loadCombinations:[],connections:[],settings:{analysisType:'linear',analysisScenarioId:'LC1',activeLoadCaseId:'LC1'},foundationReview:{items:[{id:'F1',label:'Sapata F1',nodeId:'N1',type:'footing',x:0,y:0,z:0,geometry:{B:2,L:2,h:.5}}]},foundationSSI:{contract:'foundation-ssi-model/v1',version:'0.53.4-exp',enabled:true,mode:'replace-support-dofs',items:[{foundationId:'F1',nodeId:'N1',label:'Sapata F1',enabled:true,stiffness:{kx:null,ky:null,kz:10000,krx:null,kry:null,krz:null},source:'e2e',note:''}],managedSupports:[{foundationId:'F1',nodeId:'N1',fields:{uz:{fixed:true,value:null}}}]}};await page.evaluate(p=>localStorage.setItem('astrastruct.project',JSON.stringify(p)),project);await page.reload();await page.waitForFunction(()=>document.documentElement.dataset.astraReady==='true');await page.getByTestId('analyze-button').click();const host=page.locator('.spatial3d-ssi-host');await expect(host).toHaveAttribute('data-ssi-count','1');await expect(host).toHaveAttribute('data-ssi-result-nature','physical');const overlay=page.getByTestId('spatial3d-ssi-overlay');await expect(overlay).toBeVisible();await expect(overlay.getByText(/uz -10\.00 mm/)).toBeVisible();await expect(overlay.getByText(/q̄ 25\.00 kPa/)).toBeVisible();await expect(page.getByTestId('spatial3d-ssi-status')).toContainText('1 fundação');await page.getByTestId('spatial3d-ssi-toggle').uncheck();await expect(overlay).toHaveCount(0);});